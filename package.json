{
  "name": "acceleratty",
  "version": "1.0.0",
  "description": "Simple markdown content manager with git sync for non-technical users",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-deps": "npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "express": "^4.18.2",
    "simple-git": "^3.36.0"
  }
}
